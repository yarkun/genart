(ns main
  "genart - Main entry point."
  (:gen-class)
  (:require [genart.brians-brain :as bb]))

;;; main entry point
(defn -main [& args]
  "Runs NoC sample programs."
;;; FIXME: add code to run examples
  )
